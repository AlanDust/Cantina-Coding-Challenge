source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby '2.3.3'

gem 'httparty'
gem 'byebug'
gem 'hashie'

group :development, :test do
  gem "pry"
  gem "rspec", "~> 3.7"
end
